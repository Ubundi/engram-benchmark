{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Ubundi/engram-benchmark/schemas/run_metadata.schema.json",
  "title": "RunMetadata",
  "description": "Provenance and configuration for a benchmark run.",
  "type": "object",
  "required": [
    "benchmark_release",
    "protocol_version",
    "answer_model",
    "run_id",
    "timestamp_utc",
    "config",
    "task_count",
    "prediction_count"
  ],
  "properties": {
    "benchmark_release": {
      "description": "Frozen benchmark release identifier.",
      "type": "string",
      "minLength": 1
    },
    "protocol_version": {
      "description": "Runtime protocol version identifier.",
      "type": "string",
      "minLength": 1
    },
    "answer_model": {
      "description": "Evaluated model used to generate the agent's answers.",
      "oneOf": [
        { "type": "string", "minLength": 1 },
        { "type": "null" }
      ]
    },
    "official_setting": {
      "description": "Effective official-setting identifiers for this run.",
      "type": "object",
      "required": ["split", "judge_model", "judge_passes", "judge_temperature"],
      "properties": {
        "split": { "type": "string" },
        "judge_model": { "type": "string" },
        "judge_passes": { "type": "integer", "minimum": 1 },
        "judge_temperature": { "type": "number" }
      },
      "additionalProperties": false
    },
    "run_id": {
      "description": "Run identifier in YYYYMMDDTHHMMSSz format.",
      "type": "string",
      "minLength": 1
    },
    "timestamp_utc": {
      "description": "ISO-8601 datetime with timezone.",
      "type": "string",
      "format": "date-time"
    },
    "condition": {
      "description": "Optional condition label (e.g., baseline, cortex).",
      "oneOf": [
        { "type": "string" },
        { "type": "null" }
      ]
    },
    "config": {
      "description": "Full RunConfig serialization.",
      "type": "object",
      "required": ["agent", "answer_model", "split"],
      "properties": {
        "agent": { "type": "string" },
        "answer_model": {
          "oneOf": [
            { "type": "string", "minLength": 1 },
            { "type": "null" }
          ]
        },
        "split": { "type": "string" },
        "data_path": { "oneOf": [{ "type": "string" }, { "type": "null" }] },
        "output_dir": { "type": "string" },
        "max_tasks": { "oneOf": [{ "type": "integer" }, { "type": "null" }] },
        "skip_seed": { "type": "boolean" },
        "settle_seconds": { "type": "integer", "minimum": 0 },
        "dry_run": { "type": "boolean" },
        "judge_model": { "type": "string" },
        "judge_base_url": { "type": "string" },
        "judge_passes": { "type": "integer", "minimum": 1 },
        "judge_temperature": { "oneOf": [{ "type": "number" }, { "type": "null" }] }
      },
      "additionalProperties": true
    },
    "task_count": {
      "description": "Number of tasks loaded.",
      "type": "integer",
      "minimum": 0
    },
    "prediction_count": {
      "description": "Number of predictions generated.",
      "type": "integer",
      "minimum": 0
    },
    "seed_count": {
      "description": "Number of seed phase results (0 if --skip-seed).",
      "type": "integer",
      "minimum": 0
    },
    "judgment_count": {
      "description": "Number of judgments produced (0 if dry run).",
      "type": "integer",
      "minimum": 0
    }
  },
  "additionalProperties": false
}
