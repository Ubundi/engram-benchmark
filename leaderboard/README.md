# Leaderboard

This directory defines how benchmark results should be packaged for ranking and audit.

Use [submission_format.md](submission_format.md) as the contract for submission payloads
and required provenance metadata.

Until public leaderboard automation is active, treat this as the canonical internal
submission standard.
